apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: institutoipi
# secret.enc.env NÃO entra aqui — descriptografado pelo workflow (SOPS) no Secret institutoipi-env.
resources:
  - namespace.yaml
  - postgres.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
